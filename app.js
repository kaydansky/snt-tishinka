/**
 * Inline AI chat for #entryTextarea and #reply.
 * Uses Cloudflare Turnstile + https://bot.kaydansky.ru/api.
 */

// This file is now a placeholder that loads the modularized chat components
// All functionality has been moved to:
// - turnstile.js (CAPTCHA handling)
// - chat-service.js (API communication)
// - chat-widget.js (UI components)
// - chat-init.js (initialization)

// Load all chat components
document.addEventListener('DOMContentLoaded', () => {
  // Initialize chat components - now handled by chat-init.js
  console.log('Chat module loaded');
});
